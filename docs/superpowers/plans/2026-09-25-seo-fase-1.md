# Plano — Fase 1 da auditoria SEO/GEO/AEO

**Spec:** [`docs/auditorias/2026-09-24-seo-geo-aeo.md`](../../auditorias/2026-09-24-seo-geo-aeo.md), §7.3.
**Aprovado por:** Henrique, 25/09/2026 ("autorizo a executar a fase 1").
**Teste de aceite:** `node scripts/verificar-seo.mjs [BASE_URL]` — 10 checagens, cada uma ligada
a um ID do relatório. Rodado contra a produção em 25/09 antes de qualquer código: **10/10 falharam**
(ponto de partida registrado abaixo). A Fase 1 termina quando as 10 passam na produção.

## Ponto de partida (produção, 25/09/2026)

| Checagem | Resultado antes |
|---|---|
| robots | `Disallow` em `/assets/`, `/apresentacoes`, `/professores` |
| sitemap | 148 tags `<priority>`/`<changefreq>` |
| links | 8/74 URLs alcançáveis a partir da home |
| faq-html | 8/8 respostas só no JSON-LD (Excel e CREATE 8–9) |
| aulas-html | ementa do CREATE 8–9 fora do HTML |
| h1-visivel | H1 dentro de `opacity:0` na home, no curso particular e no curso infantil |
| accept | `Accept: text/markdown` e `application/json` → 500 |
| json-ld | aggregateRating, instructor=Organization, audience na organização, hub sem ItemList |
| 404 | sem noindex e com o título da home |
| rodape | sem `<nav>`, `<address>`, `tel:` e link para `/particular` |

## ⚠️ Fora desta fase (dependem de decisão do Henrique — §6 do relatório)

- Preloader (decisão 13) · "Cursos particulares" no menu principal (decisão 6) · links do header para
  `/cursos/junior` e `/cursos/create` e nomes/idades dos programas (decisão 1) · verde dos botões
  (decisão 14) · CEP, e-mail, sábado, razão social e demais fatos (decisões 3–5, 11) · preços (decisão 7).
- A description da home **não** muda: a seção de cursos particulares entra na home nesta fase, e a
  promessa do texto passa a ser verdadeira.

## Execução em 4 grupos paralelos (cada arquivo tem um único dono)

Cada grupo trabalha numa worktree própria, a partir do commit deste plano, com um commit por pacote.

### Grupo INFRA — P1 robots/sitemap + P7 origem

Arquivos: `public/robots.txt`, `public/sitemap.xml`, `docker/server.ts`, `src/server.ts`,
`src/start.ts`, `scripts/check-sitemap.mjs` (novo), `package.json` (só o script `lint`).

- [x] Remover `Disallow: /assets/`, `/apresentacoes`, `/professores` (o noindex das páginas continua)
- [x] Sitemap sem `priority`/`changefreq`; `lastmod` fiel (`tecnico-rastreio-indexacao-12`/`-14`)
- [x] `robots.txt`/`sitemap.xml` com `max-age=3600`; remover a referência a `/sitemap-0.xml`
- [x] `scripts/check-sitemap.mjs` no `bun run lint` (rotas públicas × `<loc>`)
- [x] Brotli nível 4 (`lacuna-infra-01`)
- [x] `Accept` sem `text/html` não gera 500; `/_serverFn/*` → 404 (`lacuna-infra-04`)

### Grupo SCHEMA — P5 JSON-LD

Arquivos: `src/lib/seo.ts`, `src/components/json-ld.tsx`, `src/routes/__root.tsx`,
`src/routes/particular.index.tsx` (só o bloco de JSON-LD), `templates/novo-curso.template.tsx`.

- [x] Remover `aggregateRating` (organização e Course), `instructor`=Organization e `audience` da organização
- [x] `knowsAbout` com 8–12 tópicos; Organization/WebSite completos só na home
- [x] Escape de `<` no JSON-LD; comentários sobre rich result de FAQ atualizados
- [x] ItemList + Breadcrumb + FAQPage em `/particular`
- [x] 404 com título próprio e `noindex`

### Grupo CONTEUDO-PERF — P3 conteúdo no HTML + P6 rápidos + P8 rápidos

Arquivos: `src/components/course-page.tsx`, `src/components/course-skins/**`,
`src/components/particular-course-page.tsx`, `src/components/particular-faq*.ts(x)`,
`src/components/faq-item.tsx`, `src/components/ui/accordion.tsx`, `src/components/reveal.tsx`,
`src/components/hero-collage.tsx`, `src/components/tool-logo.tsx`, `src/assets/**`,
`src/router.tsx`, `src/routes/index.tsx` (só o hero).

- [x] FAQ dos cursos particulares e infantis sempre no HTML (`<details>`), perguntas em `<h3>`
- [x] Ementa aula a aula (`LessonAccordion`) no HTML
- [x] Hero sem `opacity:0` nos 3 templates (animação só de transform, respeitando reduced motion)
- [x] Logos de ferramentas em WebP 192 px
- [x] Temas das skins fora do chunk do hub; `defaultPreload: 'intent'`; `sizes` e prioridade no hero-collage
- [~] Espaços colados nos H1 (feito) e anos vencidos (não feito: ficaram em arquivos de outros donos — vai para a Fase 2)

### Grupo NAV-A11Y — P2 malha de links + P10 rápidos

Arquivos: `src/components/site-footer.tsx`, `src/components/site-header.tsx`,
`src/routes/cursos.index.tsx`, `src/routes/particular.tsx`, `src/components/testimonials.tsx`,
`src/components/cookie-consent.tsx`, `src/styles.css`, componentes novos, as rotas de faixa etária
(só para inserir a trilha) e `src/routes/index.tsx` (só a inserção da seção de particulares).

- [x] Rodapé com `<nav>` (programas, cursos particulares, institucional) e `<address>` com `tel:`
- [x] Chamada para `/particular` na home e em `/cursos`
- [x] Logo e mini-rodapé em `/particular` voltando ao site
- [x] `href="/blog/"`, âncoras descritivas, trilha anterior/próxima entre faixas, menu mobile no DOM
- [x] Dropdown por teclado, `aria-expanded`/`aria-controls`, sidebar com `inert` no mobile,
      `role="img"` nas estrelas, `scroll-padding-bottom` no banner de cookies, `aria-hidden` em ícones decorativos

## Integração, prova e deploy

- [x] Revisão adversarial do diff de cada grupo; correção dos bloqueantes
- [x] Merge dos 4 grupos, gate (`bun run lint` + `bun run build`, restaurar `public/og/**`)
- [x] `verificar-seo.mjs` 10/10 no servidor de produção local; screenshots conferidos
- [x] PR, merge e deploy; `verificar-seo.mjs` 10/10 na produção
- [ ] ⚠️ Purge do `robots.txt` no Cloudflare (cache de 7 dias na borda) — _Aguardando Henrique_
