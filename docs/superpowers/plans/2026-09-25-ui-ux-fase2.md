# Plano — Auditoria UI/UX · Fase 2 (sprint de consistência)

> Origem: [auditoria de 24/09/2026](../../auditorias/2026-09-24-ui-ux/README.md), seção 3 · Fase 2.
> Aprovado pelo Henrique em 25/09/2026 ("pode começar"). Objetivo: consistência, acessibilidade e
> legibilidade **sem mexer na marca**. Branch `claude-henrique/ui-ux-fase2`, a partir do `master`
> com a Fase 1 de UI/UX (#60) e a Fase 1 de SEO (#61). Cada task = 1 commit.

## Tasks

### Frente A — navegação acessível (`site-header.tsx`, `particular.tsx`)
- [x] **F177** dropdowns do header (Programas, Sobre, conta) abrem por clique/toque e teclado, com `aria-expanded`, Escape e clique fora.
- [x] **F257** drawer do `/particular` no celular: fora da tela não é focável (`inert`), foca o 1º link ao abrir, Escape fecha e devolve o foco; hambúrguer com `aria-expanded`/`aria-controls`.
- [x] tsc: erro antigo em `particular.tsx` (tipo da rota `/particular/cursos/${string}`).

### Frente B — home (`index.tsx`, `whatsapp.ts`, `decorative-elements.tsx`, `use-reveal.ts`)
- [x] **F180** botões dos programas com mensagem de WhatsApp do programa.
- [x] **F192** âncora `#investimento` + link "Ver valores" no hero.
- [x] **F289** decoração binária fora de cima do texto (hero da home e `/cursos`).
- [x] **F034** conferir o que sobrou depois do #61 (conteúdo abaixo da dobra invisível sem JS) e corrigir se ainda valer.

### Frente C — cursos infantis (`course-page.tsx`, `cursos.*.tsx`, `tech-hero.tsx`)
- [x] **F083** MetricCard não sobrepõe texto no celular (layout, sem inventar número).
- [x] rolagem horizontal de 8px em `/cursos/junior` (blob do `TechHero`).
- [x] tsc: 11 erros antigos em `cursos.academies`, `cursos.create.index`, `cursos.junior.index`.

### Frente D — cursos particulares (`particular-*`, `course-skins/**`, `particular.cursos.*.tsx`)
- [x] **F104** resultado do curso (outcome) legível: componente único, gancho + lista, sem parágrafo inteiro em negrito.
- [x] **F217** FAQ "Quanto custa?" responde com o preço real do curso (mesma fonte do card de investimento).
- [x] **F218** pílulas de categoria do hub levam à lista da área, não a um curso.
- [x] copy: "pronto para atuar como X" → "base para trabalhar como X" nos 12 cursos restantes.
- [x] código morto: template reserva de `particular-course-page.tsx` (nunca renderiza).
- [x] `id` duplicado `mkt-mark-marketing` na pele de marketing → `useId()`.

### CTO — fechamento
- [x] **F030** `DESIGN_SYSTEM.md` sincronizado com o código.
- [x] `tsc --noEmit` no gate (`bun run lint`) e `CLAUDE.md` corrigido (o build não faz type-check).

## Fora de escopo (dependem de decisão do Henrique)

- **F233 / F241 / F001 / F236** — cor dos botões (verde com texto branco reprova contraste), cor
  dos CTAs das peles, componente único de botão e estilo do "Entrar": é a decisão 2 da auditoria
  de UI/UX (= "verde dos botões" na de SEO). _Aguardando Henrique._
- **F036** — preloader da home: decisão 9. _Aguardando Henrique._
- Robótica e horário de sábado: perguntas 1 e 2.

## Verificação

`bun run lint` + `bun run build` + `tsc --noEmit` sem erros; `scripts/verificar-sidebar-fusao.mjs`
e `scripts/verificar-seo.mjs` sem regressão; screenshots conferidas; revisão adversarial antes do PR.

## Resultado (25/09)

- **Já resolvidos pelo #61 (SEO):** F177 (menus do header por teclado/toque) e a maior parte do
  F257 — só faltava foco ao abrir e devolver o foco pelo overlay.
- **Revisão adversarial** (2 revisores): 1 bloqueante e 8 "deve corrigir", todos corrigidos —
  âncoras prendiam a página com o ScrollSmoother (inclusive o "Ver os programas", anterior à
  fase), Tab saindo da gaveta, OutcomeText cortando frases (reescrito: 52/52 idênticos ao
  original), 38 promessas de copy com outra redação, "Ver valores" caindo acima do preço,
  palavra órfã nos botões de programa e contradições no `DESIGN_SYSTEM.md`.
- **Verificação:** `bun run lint` (ESLint + sitemap + `tsc`, 0 erros), `bun run build`,
  `node --test` 8/8, `scripts/verificar-seo.mjs` 10/10, `scripts/verificar-sidebar-fusao.mjs`
  318 verificações / 0 falhas.
