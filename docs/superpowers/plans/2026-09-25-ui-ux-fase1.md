# Plano — Auditoria UI/UX · Fase 1 (quick wins)

> Origem: [auditoria de 24/09/2026](../../auditorias/2026-09-24-ui-ux/README.md), seção 3 · Fase 1.
> Aprovado pelo Henrique em 25/09/2026 ("vai"). Objetivo: tirar do ar, **sem mexer na marca**,
> o que hoje quebra venda, confiança ou lei. Cada task = 1 commit.

## Tasks

- [x] **F032** 🔴 `page-transition-link.tsx` — não esconder o `<main>` quando o destino é a
      própria rota (página em branco ao clicar "Ver todos os programas" em `/cursos`).
- [x] **F255 + F159** 🔴 `particular.tsx` — sidebar amostra o que está **atrás** dela
      (`elementsFromPoint`, ignorando o próprio aside) e pinta o próprio fundo com essa cor:
      texto sempre legível, fusão preservada onde a seção vaza por baixo.
- [x] **F358** 🔴 `particular.cursos.suporte.tsx` — remover promessa de emprego em 60 dias.
- [x] **F360** 🟠 certificado: trocar "reconhecido em todo o território nacional" por
      "certificado de conclusão emitido pela Santos Tech" (skins, página de curso, JSON-LD).
- [x] **F359** 🟠 `particular.cursos.ads.tsx` — remover "formação formal", comparação com
      graduação e promessa de nível sênior (o **nome** ADS fica: decisão 5 do Henrique).
- [x] **F361** 🟠 `particular.index.tsx` — card "Pacote Office + IA" alinhado ao curso destino.
- [x] **F244** 🟠 preço/condições em `text-neutral-400` → contraste AA no card de investimento.
- [x] **F175** 🟠 entrada para `/particular` no header, rodapé e home.
- [x] **F340** 🟠 horário da home: sábado até 18h (igual a `/contato`, FAQ e schema).
- [x] **F256** 🟠 rodapé no mobile reserva a faixa do botão do WhatsApp (link "Cookies" da LGPD).
- [x] **F216** 🟠 botão do WhatsApp também nas páginas `/particular/**` no mobile.
- [x] **F322** 🟡 `robots.txt` — liberar `/assets/` para o Googlebot renderizar.

> ✅ Concluída em 25/09/2026 — cada task é 1 commit (`git log --grep "Auditoria de UI/UX"`). Complementos feitos no caminho: selo "Formado em ADS" → "Projeto de conclusão entregue", `educationalLevel` "Educação Profissional" removido do JSON-LD, rodapé das peles e contraste do item ativo da sidebar.

## Integração com o #57 (25/09)

Enquanto esta branch era feita, o PR #57 (outra sessão) entrou no `master` com uma solução
mais completa pra mesma sidebar (tom por item, amostrando o fundo atrás de cada texto, e o
verificador `scripts/verificar-sidebar-fusao.mjs`). No merge, ficou a abordagem do #57; desta
branch sobraram só os complementos que ele não cobria (reamostrar após renderizar a rota,
legenda do ADS, legenda do menu mobile em AA, botão do WhatsApp escondido com o menu
aberto). Verificador do #57 na branch integrada: 318 verificações, 0 falhas.

## Revisão adversarial (25/09)

Dois revisores independentes (correção/regressão e copy/marca) tentaram quebrar o diff.
Nenhuma regressão; 8 pontos "deve corrigir" provados e corrigidos em 5 commits: sidebar
com cor da página anterior após navegar sem rolar, regra `.sb-divider` derrubando o
contraste dos grupos abertos (4,1:1), botão do WhatsApp por cima do menu mobile, falta do
link "Cookies" no `/particular` (LGPD), "adultos" no item novo do header e na home
(contra a decisão de 23/09 — particular é qualquer idade), "primeiro emprego" na landing,
enquadramento de curso superior no ADS, certificação Blackmagic "inclusa" no DaVinci
(`F379`), "engenheiro" no AutoCAD e "43 cursos" no texto para LLMs.

## Fora de escopo (YAGNI)

- **F181** (hero promete robôs × FAQ nega robótica): **bloqueado** — `/cursos/academies` vende
  uma Robotics Academy com kits físicos, então não dá pra saber qual texto está certo sem o
  Henrique. _Aguardando Henrique._
- **F323** (`www` sem DNS): passo privilegiado na Cloudflare, já registrado em `PENDENCIAS.md`.
- Tudo das Fases 2 e 3 (verde do CTA, catálogo infantil único, template do /particular etc.).

## Verificação

`bun run lint` + `bun run build` sem erros (restaurar `public/og*` depois do build) e
screenshots conferidas: sidebar nas 8 peles + landing, rodapé mobile, header, home e card
de investimento.
